apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: nunmai-portal
resources:
  - postgres.yaml
  - litellm.yaml
configMapGenerator:
  - name: litellm-config
    files: [litellm-config.yaml]
generatorOptions:
  disableNameSuffixHash: true
