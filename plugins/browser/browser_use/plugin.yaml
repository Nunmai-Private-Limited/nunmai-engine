name: browser-browser-use
version: 1.0.0
description: "Browser Use (https://browser-use.com) cloud browser backend. Supports both direct BROWSER_USE_API_KEY and the managed Nunmai tool gateway. Also powers the 'Nunmai Subscription' UX flow that bills usage to a Nunmai subscription."
author: NousResearch
kind: backend
provides_browser_providers:
  - browser-use
