name: langfuse
version: "1.0.0"
description: "Optional Langfuse observability for Nunmai — traces conversations, LLM calls, and tool usage. Opt-in via `nunmai plugins enable observability/langfuse` or `nunmai tools → Langfuse Observability`."
author: NousResearch
requires_env:
  - NUNMAI_LANGFUSE_PUBLIC_KEY
  - NUNMAI_LANGFUSE_SECRET_KEY
hooks:
  - pre_api_request
  - post_api_request
  - api_request_error
  - pre_llm_call
  - post_llm_call
  - pre_tool_call
  - post_tool_call
  - on_session_finalize
  - on_session_end
  - subagent_start
  - subagent_stop
